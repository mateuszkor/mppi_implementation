# config/params.yaml
simulation:
  name: "hand_fixed"  # Options: cartpole, humanoid, etc.
  path: "xmls/shadow_hand/scene_right.xml"

mppi:
  n_steps: 100
  n_rollouts: 10
  lambda: 0.8
  initial_control: "zeros"     # random or zeros

costs:
  control_weight: 1e-3
  terminal_weight: 1.0


